// Simulate user input with prompt (assumed)
const size = 5; // Replace with prompt or dynamic input in real usage
const arr = [];  // Create an empty array
for (let i = 0; i < size; i++) {
    arr[i] = i;  // Assign values to the array
}
console.log(arr.join(" "));  // Print array values separated by space
